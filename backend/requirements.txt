fastapi
uvicorn[standard]
python-dotenv
pydantic
httpx
# Драйвер + пул соединений. Заменил вызовы psql подпроцессом: стенд терял
# около половины новых соединений, а аргументы командной строки на Windows
# конвертировались в ANSI-кодовую страницу и убивали кириллицу в запросах.
psycopg[binary,pool]
pyjwt
# bcrypt напрямую, без passlib: passlib 1.7.4 не обновлялся с 2020 года и
# несовместим с bcrypt >= 5.0 (его проба бэкенда шлёт пароль длиннее 72 байт
# и падает с ValueError).
bcrypt
# Разбор PDF с приложениями к Правилам приёма ИГУ. Приложению не нужен —
# нужен сборщику датасета (backend/scripts/build_dataset.py), который иногда
# запускают внутри контейнера: docker compose exec backend python
# scripts/build_dataset.py. Без него команда падает на импорте.
pypdf
