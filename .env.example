DATABASE_URL=
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# Секрет для подписи JWT (HS256). Обязателен — значения по умолчанию в коде нет.
# Сгенерировать: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
API_KEY=
FOLDER_ID=
MODEL_NAME=
API_URL=
AGENT_SYSTEM_PROMPT=
